
  # Cinema Festival Schedule

  This is a code bundle for Cinema Festival Schedule. The original project is available at https://www.figma.com/design/md7MIKfDXruqT3f8xua4iS/Cinema-Festival-Schedule.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  